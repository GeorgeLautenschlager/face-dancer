"""Mock hosts — test doubles implementing the host side of the protocol contract.

Real hosts (game engines, DM agents, human-run Discord sessions) are external;
these mocks exist for unit and integration testing only.
"""
